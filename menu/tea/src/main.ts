const makeTea = () => {
  console.group("🍵 Готовим чай:");
  const tea = "[листовой чай + вода (горячая)]"
  console.log(tea);
  console.groupEnd();
  console.log("🍵 Чай готов!");
}

makeTea()
